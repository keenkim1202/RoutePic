# RoutePic — 구현 계획

| 항목 | 값 |
|---|---|
| 상태 | 계획 v1 — **M0·M1·M3·M4·M6(코어) 완료** (2026-08-07) |
| 작성일 | 2026-08-07 |
| 진행 | 아래 §요약 표 참조. 남은 것은 전부 **물리 장비·계정·사람**이 필요한 항목 |
| 선행 문서 | [`DESIGN.md`](./DESIGN.md) — 설계 v0.2 (Codex 교차검토 반영, 미결 0건) |
| 전제 | 1인 개발, iOS 17+, §13 결정 4건 확정됨 |

> 작업일 추정은 전부 `[추정]`이다. 1인 풀타임 기준이며, 실측(T-1)과 스파이크 결과에 따라 M5 이후가 크게 흔들릴 수 있다.

---

## 0. 이 계획의 축

### 0.1 순서를 정한 원칙 세 가지

**① `ShapeKit`을 맨 앞에 둔다.**

스파이크(§7.7)에 필요한 건 "GPX → 16방향 컨트롤 이미지 PNG"다. 그건 정확히 `ShapeKit`의 산출물이다. 스파이크용 파이썬 스크립트를 따로 짜면 앱 코드와 결과가 달라져서 **스파이크가 검증한 것이 앱에 없게 된다.** `ShapeKit` + 작은 CLI 타깃을 먼저 만들면 같은 코드로 검증하고 같은 코드로 출시한다.

**② AI 종속 작업을 최대한 뒤로 민다.**

요구 1·2·4·5·6은 AI와 무관하다. 요구 3만 스파이크 판정에 걸려 있다. M4까지 끝내면 **AI가 0번 성공해도 출시 가능한 제품**이 있다. 이게 이 프로젝트의 안전장치다.

**③ `LocationSource` 프로토콜로 OQ-D를 우회한다.**

T-1(클래식 vs 모던 실측)이 안 끝나도 `RouteKit`의 나머지(필터·저널·복구·세션)는 진행할 수 있다. 프로토콜 뒤에 두 구현을 넣고, T-1은 **어느 구현을 기본값으로 할지만** 정한다. 실측이 크리티컬 패스를 막지 않는다.

### 0.2 의존 그래프

```
M0 준비 ─┬─────────────────────────────────────────────────┐
         │                                                 │
         ├─► M1 ShapeKit ──┬─► M2-A 스파이크 (5~7일) ──────┼─► M5 판정
         │                 │                               │      │
         │                 └─► (앱에서 재사용)              │      ▼
         ├─► M2-B T-1 실측 ─────► (기본 구현 확정)          │   M6 생성
         │                                                 │      │
         └─► M3 RouteKit ─────────► M4 Data·화면·로컬카드 ──┘      ▼
                                       ▲                       M7 마감
                                       └─ 여기까지가 출시 가능 최소 제품
```

**크리티컬 패스:** M0 → M1 → M3 → M4 → (M5) → M6 → M7
**병렬 가능:** M2-A(스파이크)와 M2-B(T-1)는 M3·M4와 동시 진행. 스파이크는 손이 많이 가는 작업이 아니라 **대기 시간이 긴** 작업이다(생성 호출·평가자 모집).

---

## M0 — 준비 `[추정 1일]`

### 작업

| # | 내용 |
|---|---|
| 0.1 | Xcode 프로젝트 생성. iOS 17.0 최소, SwiftUI 라이프사이클, Swift 6 언어 모드 |
| 0.2 | 로컬 SPM 2개 스켈레톤: `Packages/RouteKit`, `Packages/ShapeKit` (§10.1) |
| 0.3 | CLI 타깃 `shapelab` 추가 — macOS 실행 파일, `ShapeKit` 의존. 스파이크 도구이자 골든 테스트 러너 |
| 0.4 | `git init`, `.gitignore`(Xcode + `*.gpx` 대용량 제외 정책 결정) |
| 0.5 | **GPX 45개 수집 시작** — 아래 |

### 0.5가 병목이다

스파이크 표본은 §7.7이 요구하는 대로 **모드별 15개 × 3모드, 길이·굴곡도 분위수로 층화**해야 한다. 조달 경로:

1. 본인 Apple Health / Strava 내보내기 (있다면 최우선 — 실제 분포)
2. 공개 GPX 데이터셋
3. 부족분은 M3 완료 후 직접 기록 (러닝·워킹·드라이빙 각 며칠)

> **주의:** 3번에 의존하면 스파이크가 M3 뒤로 밀린다. **M0에서 1·2로 최대한 확보**하고, 모자란 모드만 3번으로 채운다. 45개를 다 못 모으면 층화를 유지한 채 30개(모드별 10)로 시작하고 예비 분석 후 늘린다.

### DoD
- [ ] 빈 앱이 시뮬레이터에서 실행
- [ ] `swift test`가 두 패키지에서 통과 (빈 테스트라도)
- [ ] `shapelab --help`가 동작
- [ ] GPX ≥ 30개 확보, 모드·길이·굴곡도 메타 표 작성

---

## M1 — ShapeKit `[추정 3일]` ★ 크리티컬 패스 시작

순수 함수만. I/O·CoreLocation·SwiftUI 의존 없음. 여기가 앱의 핵심 자산이다(§10.1).

### 작업

| # | 타입 / 함수 | 내용 | 설계 근거 |
|---|---|---|---|
| 1.1 | `RoutePoint`, `RouteSegment`, `SegmentKind` | 값 타입. `.moving` / `.paused` / `.gap` | §8.1 |
| 1.2 | `PolylineCodec` | 델타 + zigzag varint 인코딩·디코딩 | §8.2 |
| 1.3 | `resample(_:everyMeters:)` | 등거리 리샘플링 | §6 |
| 1.4 | `simplifyRDP(_:targetVertexRange:)` | ε 적응적, 60~200 정점 | §6 |
| 1.5 | `ENUProjection` | centroid 기준 접평면. 날짜변경선 ±360 정규화, \|lat0\|>85° 거부 | §6.1 |
| 1.6 | `normalize(to:padding:)` | bbox → 1024², 종횡비 유지, 8% 패딩 | §6 |
| 1.7 | `smoothCatmullRom(_:)` → `CGPath` | **렌더 전용.** 원본 불변 | §5.3 |
| 1.8 | `ShapeFingerprint` | 폐합도·종횡비·굴곡도·회전각 시퀀스·점유격자(32²)·골격 분기 수·자기교차 | §6.2 |
| 1.9 | `ControlImageRenderer` | 검정 배경/흰 선, 선폭 8~14 px. **16방향(8회전 × 미러)** 출력 | §4.2, §7.1 |
| 1.10 | `RouteTrimmer` | 시작·끝 트림. 40% 상한, 루프 예외, 300 m 미만 차단 | §8.4 |

### 검증 (T-4 골든 테스트)

```
shapelab render <in.gpx> --out ./frames/   # 16방향 PNG
shapelab fingerprint <in.gpx>              # 지문 JSON
shapelab golden --fixtures ./fixtures/     # 회귀 비교
```

- 고정 GPX 20종 → 렌더 PNG 픽셀 해시 비교
- **경계 케이스 픽스처를 반드시 포함:** 점 1개 / 점 2개 / 완전 직선 / 완전 폐합 루프 / 8자 교차 / 날짜변경선 통과 / 고위도 / 좌표 중복 / gap 다수

### DoD — **완료 2026-08-07**
- [x] 골든 테스트 통과, 경계 케이스 9종 포함 (`GoldenTests`, SHA-256 기준값 기록)
- [x] `PolylineCodec` 왕복 무손실 (1e-6°, 실측 **3.86 B/point** — §8.2 예산 4~6 B 내)
- [x] 합성 픽스처 전부 크래시 없이 16방향 렌더
- [x] **97개 테스트 통과** (`swift test`)
- [ ] 실제 GPX 45개 렌더 — **M0.5 조달 대기** (합성 픽스처로 대체 검증 완료)

### M1이 실제로 잡은 것 — 설계 문서만으로는 안 보였던 5건

| # | 발견 | 조치 |
|---|---|---|
| 1 | **트림이 세그먼트 구조를 파괴** → gap이 직선으로 이어짐 (§5.4 정면 위반) | `Route.slice(_:)` 신설, 트림이 유지 인덱스 범위 반환 |
| 2 | 파이프라인 **순서 오류** — 투영이 리샘플·단순화 뒤 (평면이 먼저 필요) | 투영을 앞으로 |
| 3 | **"골격 분기 수"가 어떤 해상도에서도 0** — X 교차가 2×2 블록으로 래스터화돼 junction 픽셀이 없음 | 돌출부 수(반경 국소 최대, 폐합 시 순환 스캔)로 대체 |
| 4 | 볼록비가 8자에서 부호 상쇄로 ~0 | 자기교차 0 조건 추가 |
| 5 | 자기교차가 **끝점 공유를 교차로 오인** | 끝점 공유 배제 |

`DESIGN.md` v0.3에 전부 반영. 1번은 프라이버시·정확성 양쪽에 걸린 **높음** 등급이었고, 문서 검토(Claude·Codex 양쪽)에서는 잡히지 않았다.

### 산출물

```
Packages/ShapeKit/
  Sources/ShapeKit/      13 파일 — Route · Geometry · ENUProjection · Resample ·
                         Simplify · Normalize · Smoothing · Orientation ·
                         OccupancyGrid · ShapeFingerprint · PolylineCodec ·
                         RouteTrimmer · ControlImageRenderer · ShapePipeline · GPXDocument
  Sources/shapelab/      CLI — render / fingerprint / info
  Tests/ShapeKitTests/   8 파일, 97 테스트
```

```
shapelab render <route.gpx> --out <dir> [--mode walk|run|drive] [--trim m] [--only n]
shapelab fingerprint <route.gpx>
shapelab info <route.gpx>
```

---

## M2-A — 스파이크 `[추정 5~7일, 병렬]`

M1 완료 즉시 시작. **`shapelab`이 뽑은 이미지를 그대로 쓴다.**

### 순서 (의존성 있음)

```
SP-3 선의 의미 A/B/C  ──►  SP-4 구속 강도  ──►  SP-2 회전  ──►  SP-1 본 평가  ──►  SP-5 why 노출
   (조건 방식 결정)        (강도 확정)        (효과 측정)      (S2/S3/로컬 비교)    (귀속감 A/B)
```

SP-3·4·2는 **작은 표본(10개)으로 빠르게** 돌려 파라미터를 좁힌다. SP-1만 전체 표본 + 평가자 5인.

### 산출물

| # | 내용 |
|---|---|
| A.1 | `spike/` 디렉터리 — 호출 스크립트, seed 고정, 결과 매니페스트 |
| A.2 | 조건 방식·구속 강도·회전 정책 확정값 |
| A.3 | §4.3 4개 척도 결과표 (Chamfer/Hausdorff 자동 계산 + 블라인드 평가) |
| A.4 | **§7.6 P50/P95 원가표** — 실제 호출 로그 기반. 재생성률 R·실패율 F 포함 |
| A.5 | **공급자 보존·학습·리전·삭제 SLA 문서** (§11) — 계약 확인 전 M6 착수 금지 |

### 병행 확인 (코드 아님, 리드타임 김 → 일찍 시작)
- Replicate / fal 계정·요금·rate limit
- VLM 공급자 이미지 입력 토큰 단가 (16장 입력이 비용에서 지배적)
- 데이터 처리 정책 문서 확보

---

## M2-B — T-1 위치 API 실측 `[추정 1.5일, 병렬]`

OQ-D 해소. M3의 **기본 구현 선택**만 결정하므로 M3를 막지 않는다.

### 방법
동일 기기·동일 코스 3회 × 2구현. 측정:

| 지표 | 방법 |
|---|---|
| 좌표 누락률 | 예상 fix 수 대비 실 수신 |
| 백그라운드 지속 | 앱 백그라운드 30분 후 계속 수신되는가 |
| 종료 후 재개 | jetsam 유발 후 재실행 시 동작 |
| 블루바 잔존 | 세션 종료 후 인디케이터가 사라지는가 |
| 배터리 | 1시간당 %p |
| 메모리 | 누수 여부 (Instruments) |

### DoD
- [ ] 결정 + 근거를 `DESIGN.md` §5.1에 기록 (OQ-D 확정으로 갱신)
- [ ] 진 쪽 구현도 `LocationSource` 뒤에 남긴다 (iOS 버전별 회귀 대비)

---

## M3 — RouteKit `[추정 3일]`

### 작업

| # | 타입 | 내용 | 근거 |
|---|---|---|---|
| 3.1 | `protocol LocationSource` | `AsyncStream<CLLocation>` + 시작/중지/권한 | OQ-D 우회 |
| 3.2 | `ClassicLocationSource` / `ModernLocationSource` | 두 구현 | §5.1 |
| 3.3 | `LocationFilterChain` | 신선도 → 워밍업(정확도 기준) → 정확도 게이트 → 속도 판정(음수 speed 처리) → 이상치 확인(다음 fix로) → 거리 게이트 | §5.3 |
| 3.4 | `SessionJournal` | 프레임 `[magic][len][payload][crc32]`, flush 5프레임/10초/백그라운드 진입, 체크포인트 5분 | §5.4 |
| 3.5 | `JournalReader` | CRC 실패 프레임에서 중단, 앞까지 채택 |  |
| 3.6 | `RecordingSession` | 상태 머신 `idle→recording→paused→finishing→finished`, gap 감지(모드별 임계) | §5.4·5.5 |
| 3.7 | `SessionRecovery` | 앱 시작 시 미완료 저널 스캔 → 복구 후보 | §5.4 |
| 3.8 | `ActivityStatistics` | 거리·movingDuration·pace·elevationGain(수직정확도 ≤10 m, 3 m 히스테리시스) | §9 |

### 검증

| 테스트 | 방법 |
|---|---|
| T-6 시뮬레이션 | Xcode GPX 재생 + **이상치·점프·정확도 악화 주입** |
| T-3 종료 복구 | 강제 종료 / 재부팅 / jetsam / 앱 업데이트 각각 실측 → §5.4 매트릭스 채우기 |
| 필터 단위 테스트 | 음수 speed, 정확도 -1, 동일 타임스탬프, 시간 역행 |

### DoD
- [ ] 실기기 30분 백그라운드 세션에서 좌표 누락 없음
- [ ] 강제 종료 후 재실행 시 복구 시트가 뜨고 좌표가 보존됨
- [ ] §5.4 보장 매트릭스의 `[미검증]`이 전부 실측값으로 교체됨
- [ ] 저널 파일에 CRC 파손을 주입해도 앞부분이 복구됨

---

## M4 — Data · 화면 · 로컬 카드 `[추정 5일]` ★ 출시 가능선

여기까지 끝나면 **AI 없이 요구 1·2·4·5·6이 충족된 제품**이 나온다.

### 4a. 저장 계층 `[1.5일]`

| # | 내용 | 근거 |
|---|---|---|
| 4.1 | `SchemaV1` (`VersionedSchema`) — `Activity`·`Artwork`, `#Index`, cascade + inverse, 동기화 앵커(`remoteID`/`updatedAt`/`deletedAt`) | §8.1 |
| 4.2 | `ActivityRepository` — 페이징 조회(startedAt 역순), 필터 |  |
| 4.3 | `LocalArtworkStore` 프로토콜 + 파일 구현 — 임시 write → fsync → 원자적 rename → DB insert | §8.1, §13.1 |
| 4.4 | `OrphanCleaner` — 앱 시작 시 백그라운드, 24시간 이상 된 고아 파일만 | §8.1 |
| 4.5 | `DerivedRouteProvider` — 원본/표시용/공유용 3종 분리. 공유용은 **항상 재계산** | §8.4 |

### 4b. 기록 화면 `[1.5일]`

- 모드 선택 + 시작 / 진행(지도 + 실시간 폴리라인 + 지표) / 일시정지 / 종료 요약
- **드라이빙 모드 안전:** 큰 글씨, 버튼 2개, 메모·생성은 정차 판정 후에만 활성 (§9)
- 복구 시트

### 4c. 컬렉션·상세·공유 `[2일]`

| # | 내용 |
|---|---|
| 4.6 | 컬렉션 3열 그리드 ⇄ 1열 타임라인, 모드·즐겨찾기 필터, 월별 점프, 빈 상태 |
| 4.7 | 상세 — 지도 스냅샷(표시용 파생), 통계, 기록 누락 구간 표시, 메모 인라인 편집 |
| 4.8 | **로컬 카드 렌더러** — 경로 선 + 통계 + 그라디언트. AI 폴백이자 M4의 주력 산출물 |
| 4.9 | 공유 — `ShareLink`, 1:1 / 4:5 / 9:16, 포함 항목 체크박스, **EXIF/GPS 전량 제거** |
| 4.10 | GPX 내보내기 |

### DoD
- [ ] 기록 → 저장 → 컬렉션 → 상세 → 공유 전 경로가 실기기에서 동작
- [ ] 공유한 이미지에 위치 메타데이터가 **없음을 exiftool로 확인**
- [ ] 활동 삭제 시 이미지·썸네일 파일까지 사라짐
- [ ] 100개 활동에서 컬렉션 스크롤 60fps
- [ ] **이 시점에 TestFlight 배포 가능** (AI 없는 v0.9)

---

## M5 — 판정 게이트 `[0.5일]`

M2-A 결과를 §7.7 판정표에 넣는다. **여기서 M6의 내용이 결정된다.**

| 스파이크 결과 | M6 |
|---|---|
| S3가 인식성·귀속감 둘 다 우위 | 계획대로 S3 구현 |
| 귀속감만 우위, 인식성은 S2 압도 | **사용자에게 재질의** (OQ-C 재개) — 문서에 명시된 분기 |
| 둘 다 열위 | S2로 피벗. Stage 2를 T2I로 교체 (프록시·쿼터·UI는 대부분 재사용) |
| 로컬 카드가 대등 | **AI를 v1에서 제외.** M4 결과물로 출시하고 M6는 v2로 |

> 이 게이트를 형식적으로 통과시키지 않는다. **"AI 제외"가 진짜 선택지**여야 게이트가 의미 있다.

---

## M6 — 생성 파이프라인 `[추정 6일]` (S3 채택 시)

### 6a. 프록시 `[2일]`

Cloudflare Workers (또는 Supabase Edge). **좌표는 절대 받지 않는다.**

| # | 내용 | 근거 |
|---|---|---|
| 6.1 | `POST /jobs` — `Idempotency-Key` 필수, App Attest 검증, 쿼터 **예약** | §8.3 |
| 6.2 | 잡 상태 머신 `queued→running→succeeded/failed/cancelled/expired`, 실패 시 **환불** | §8.3 |
| 6.3 | `GET /jobs/:id` — 폴링 |  |
| 6.4 | VLM 호출(16장 + 지문) → 생성 호출 | §7.2·7.3 |
| 6.5 | 객체 스토리지 TTL 7일 + 라이프사이클 삭제 + 삭제 검증 잡 | §11 |
| 6.6 | **로그 스크럽** — 요청 본문·서명 URL 제거, 기기 해시만 | §11 |
| 6.7 | 기기당 동시 요청 1 제한 |  |

### 6b. 앱 클라이언트 `[1.5일]`
- `GenerationClient` — 잡 생성·폴링(백오프 2→5→10s), 백그라운드 전환 후 재조회
- 실패·오프라인·쿼터 소진 → 로컬 카드 폴백 (§4.4)
- 짧은 경로·직선 사전 차단 (굴곡도 임계)

### 6c. 생성 UI `[1.5일]`
- 형상 프리뷰(선택된 방향으로 회전, Reduce Motion 시 생략)
- `why` 노출, 주제 칩 3개, 스타일 프리셋
- 진행 상태 + 취소
- 후보 2장 선택, 경로 오버레이 토글
- **트림 소급 배지** — 트림 강화 시 기존 아트워크에 "이전 설정으로 생성됨" (§8.4)

### 6d. 쿼터·구독 `[1일]`
- StoreKit 2 구독, 서버 `Transaction` 검증, `originalTransactionID` 앵커
- 무료 상한 N = §7.6 P95 원가로 역산
- **페이월은 AI 생성에만.** 기록·컬렉션·공유·로컬 카드는 영구 무료 (§8.3)

### DoD
- [ ] 생성 실패를 강제 주입했을 때 쿼터가 환불됨
- [ ] 같은 `Idempotency-Key` 재전송 시 중복 과금 없음
- [ ] 비행기 모드에서 기록은 정상, 생성만 큐잉
- [ ] 프록시 로그에 좌표·형상·서명 URL이 없음을 직접 확인

---

## M7 — 마감 `[추정 4일]`

| # | 내용 | 근거 |
|---|---|---|
| 7.1 | §14.1 상태 매트릭스 전부 — 권한 거부·축소, 정밀위치 OFF, 저전력, GPS 음영, 비행기, 디스크 부족 | §14.1 |
| 7.2 | T-7 접근성 — VoiceOver 전 화면(생성 이미지 대체텍스트 = `subject` + `why`), 최대 Dynamic Type, Reduce Motion | §9 |
| 7.3 | T-2 배터리·발열·메모리 — 1/3/6시간. 목표 초과 시 §5.6 완화 순서 적용 | §5.6 |
| 7.4 | T-5 마이그레이션·손상 테스트 | §14.2 |
| 7.5 | 전체 데이터 삭제(DB+이미지+썸네일+저널+서버 객체 연쇄), 내보내기(GPX+ZIP) | §11 |
| 7.6 | Privacy Manifest, 영양성분표, 재식별 가능성 고지 문구 | §11 |
| 7.7 | 로케일 — km/mi, 시간대 변경, 페이스 표기 | §9 |
| 7.8 | 저장공간 한도·썸네일 eviction | §14 MISSING 5 |
| 7.9 | 생성 이미지 이용권·저작권 고지 | §14 MISSING 13 |

---

## 요약

| M | 내용 | 추정 | 병렬 | 끝나면 |
|---|---|---|---|---|
| M0 | 준비 + GPX 수집 | 1일 | | |
| M1 | **ShapeKit** | 3일 | | 스파이크 착수 가능 |
| M2-A | 스파이크 | 5~7일 | ‖ M3·M4 | 방향 확정 |
| M2-B | T-1 실측 | 1.5일 | ‖ M3 | OQ-D 해소 |
| M3 | RouteKit | 3일 | | 기록이 됨 |
| M4 | Data·화면·로컬카드 | 5일 | | **TestFlight 가능** |
| M5 | 판정 게이트 | 0.5일 | | M6 내용 결정 |
| M6 | 생성 파이프라인 | 6일 | | 요구 3 충족 |
| M7 | 마감 | 4일 | | 출시 준비 |

**크리티컬 패스 합계 `[추정]`: 22.5 작업일** (M2-A·M2-B는 병렬이라 미포함)
1인 풀타임 기준 **약 4.5~6주**. 스파이크 대기, 실기기 테스트 재시도, App Store 심사는 별도.

---

## 실제 진행 현황 (2026-08-07)

| M | 상태 | 검증 방법 |
|---|---|---|
| M0 | ✅ (GPX 조달만 미완) | `swift build` · `shapelab --help` |
| M1 ShapeKit | ✅ | **97 테스트 통과**, 골든 이미지 해시 고정, 실제 렌더 육안 확인 |
| M2-A 스파이크 | ⛔ **차단** | 실제 GPX 45개 + 공급자 계정 + 평가자 5인 필요 |
| M2-B T-1 | ⛔ **차단** | 실기기로 같은 코스 3회 이동 필요 |
| M3 RouteKit | ✅ (T-3 실기기 제외) | **58 테스트 통과** |
| M4 Store·앱 | ✅ | **17 테스트 통과**, iOS 시뮬레이터 빌드·실행 확인 |
| M5 판정 게이트 | ⛔ **차단** | M2-A 결과 대기 |
| M6 생성 | 🟡 **코어 완료** | **26 테스트 통과**. 프록시 코드 존재. **엔드투엔드 미검증** — API 키 없음 |
| M7 마감 | 🟡 부분 | 상태 배너·접근성·삭제·EXIF 제거·GPX 내보내기 완료. 배터리(T-2)·실기기 테스트 차단 |

**합계 198 테스트 통과** (ShapeKit 97 · RouteKit 58 · Store 17 · Generation 26).

### 차단 항목 — 사용자만 해소 가능

| # | 필요한 것 | 막고 있는 것 |
|---|---|---|
| 1 | GPX 45개 (Apple Health / Strava 내보내기) | M2-A → M5 → M6 착수 판단 |
| 2 | 공급자 계정 + **데이터 보존·학습·리전·삭제 SLA 문서** | M6 프로덕션 (§11이 명시적 차단 사유로 지정) |
| 3 | 실기기 + Apple Developer 계정 | M2-B, M3 T-3, M7 T-2 |
| 4 | 평가자 5인 | SP-1 블라인드 비교 |

### 구현이 잡아낸 설계·구현 오류 누계 **17건**

| M | 건수 | 대표 |
|---|---|---|
| M1 | 5 | 트림이 gap 구조 파괴 · 골격 분기 수가 항상 0 · 투영 순서 |
| M3 | 4 | 서 있으면 auto-pause 영구 미발동 · 확인된 이상치 유실 · 고도 히스테리시스 3 m가 GPS 노이즈보다 작음 |
| M4 | 4 | `#Index`가 iOS 18 필요 · `FileManager` 비-Sendable · CoreText 키 · actor 격리 |
| M6 | 3 | 빈 성공에 과금 · 서버 시계로 만료 판정 · `ShapeFingerprint` public init 부재 |
| 자체리뷰 | 5 | **세션 시작 fix 유실 레이스** · 모드 필터가 페이징 파괴 · `.constant` 바인딩 · `try!` · 데드코드 |

문서 검토(Claude·Codex 2회)에서는 **한 건도** 나오지 않았던 유형이 다수다.

---

## 리스크와 조기 경보

| 리스크 | 언제 드러나나 | 대응 |
|---|---|---|
| **S3가 안 됨** | M2-A (SP-1) | M5 게이트. M4까지 이미 출시 가능하므로 치명적이지 않음 |
| GPX 45개를 못 모음 | M0 | 30개(모드별 10)로 층화 유지해 시작, 예비 분석 후 확대 |
| 공급자 데이터 정책이 부적합 | M2-A 병행 확인 | **M6 착수 전 차단 사유.** 공급자 교체 또는 AI 보류 |
| 배터리 목표 초과 | M7 (T-2) | §5.6 완화 3단계. 늦게 발견되므로 **M3 완료 시 1시간 예비 측정** 권장 |
| 백그라운드 추적이 불안정 | M2-B / M3 | 두 구현이 다 문제면 앱 정체성이 흔들림 → 조기 발견이 중요해 M2-B를 앞에 둠 |
| 원가가 예상 초과 | M2-A (A.4) | 무료 상한 N 하향, 후보 2장 → 1장, 저해상도 1차 생성 후 업스케일 |

### 지금 당장 시작할 것 (리드타임 순)

1. **GPX 수집** — Apple Health / Strava 내보내기 (조달이 제일 오래 걸림)
2. **공급자 계정 + 데이터 정책 문서 확보** — 회신 대기가 있을 수 있음
3. M0 프로젝트 스켈레톤

---

## 부록. 문서 갱신 규칙

구현이 시작되면 **코드가 SoT**다. `DESIGN.md`는 다음 시점에만 갱신한다:

| 시점 | 갱신 대상 |
|---|---|
| M2-B 완료 | §5.1 OQ-D 확정, §14.2 T-1 결과 |
| M3 완료 | §5.4 보장 매트릭스의 `[미검증]` → 실측값 |
| M2-A 완료 | §7.1 조건 방식, §7.3 구속 강도, §7.6 원가표, §11 공급자 정책 |
| M5 | §4 가설 → 확정 또는 피벗 기록 |
| M7 | §5.6 배터리 실측값 |

`[가설]`·`[미검증]` 라벨이 붙은 항목이 실측으로 바뀔 때마다 **라벨도 함께 바꾼다.** 라벨이 남아 있으면 아직 안 정해진 것이다.
