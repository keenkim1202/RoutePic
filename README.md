# RoutePic

걸어온 길을 AI가 해석해 그림으로 남기는 iOS 앱.

```
이동 기록          경로 → 형상            AI 해석·생성           기록·공유
──────────      ─────────────         ─────────────        ─────────────
GPS 폴리라인  →  단순화·정규화     →   무엇처럼 보이나(VLM)  →  카드 렌더
              →  컨트롤 이미지      →   형상 구속 생성        →  피드/컬렉션
              →  형상 지문                                   →  공유
```

## 어려운 지점은 AI 생성이 아니다

임의의 GPS 경로는 대체로 아무것도 닮지 않았다. 계획해서 달린 경로는 이미 고양이 모양이지만,
어제 퇴근길은 그냥 꺾인 선이다. 그래서 이 앱의 성패는 이미지 생성 API를 붙이는 데 있지 않고
**"안 닮은 모양을 무엇으로 만들 것인가"** 라는 제품 결정에 달려 있다.

시장 조사에서 유사물을 찾지 못했다. 기존 앱은 전부 역방향(모양을 먼저 정하고 그에 맞는 경로를
계획)이거나, 순방향이어도 선을 예쁘게 꾸미는 데 그친다. 그건 기회이자 경고다 — 아무도 안 하는
이유가 "안 되어서"일 가능성을 설계서 §4와 §7.11이 정면으로 다룬다.

**축은 하나다. 구속을 올리면 기괴해지고, 내리면 내 경로가 아니게 된다.** 파이프라인을 2단으로
나눠도 이 트레이드오프는 사라지지 않는다. 스위트스팟이 넓어질 뿐이다.

## 실측이 설계를 뒤집은 것

문서를 읽거나 코드를 써서가 아니라 실제로 이미지를 만들어 보고 드러났다. 근거는
[`OnDevice/SPIKE-RESULTS.md`](OnDevice/SPIKE-RESULTS.md).

| 발견 | 내용 |
|---|---|
| **seed 분산이 조건 차이보다 컸다** | 모든 스파이크가 셀당 seed 1개를 전제했는데, 그러면 아무것도 구분하지 못한다. 조건당 seed ≥ 5로 전면 개정 |
| **병목이 예상과 반대였다** | 인식성은 이미 나온다(5~6/6). 무너진 쪽은 경로 귀속감(0/6). 인식성을 순위 척도에서 게이트로 강등 |
| **스타일 프리셋은 톤이 아니라 손잡이였다** | 라인아트는 경로를 정확히 추적하지만 주제를 거의 만들지 않고, 플랫 벡터는 그 반대다. 구속 강도와 독립 변수로 둘 수 없다 |
| **강도 축은 단조가 아니다** | 42셀 전수 측정에서 `edgeToRoute`가 단조 증가하지 않았고, 준비한 지표는 이 축을 서열화하지 못했다 |

초판 결론은 셀당 seed 1개로 6건을 돌려 "구불구불한 경로엔 동물이 안 나온다"였다. seed를 6개로
늘리자 뒤집혔다. **이 개정 자체가 한 번 틀렸다**는 기록을 설계서에 남겨 두었다.

## 어떻게 측정하나

"실루엣이 경로를 따라간다"는 주장이 아니라 측정 대상이다.

| 척도 | 방법 | 상태 |
|---|---|---|
| 경로 귀속감 | 경로 선 오버레이 없이 "이게 내 경로 같은가" | **0/6 — 병목** |
| 형상 충실도 | 결과에서 주 실루엣을 추출해 원 폴리라인과 Chamfer / Hausdorff 거리 | 귀속감의 기계적 대리 지표 |
| 인식성 | 블라인드 평가자에게 이미지만 보여주고 자유응답 | 5~6/6 — 하한 통과 조건 |
| 만족도 | 5점 척도 + 재생성 의향 | 미측정 |

단일 순위 척도로 만들지 않는다. 귀속감만으로 줄 세우면 충실하지만 보기 싫은 결과가 이기고,
평균은 사용자가 매 생성에서 겪는 실패율을 가린다. 셋의 파레토로 결정하고, 게이트에는 수치 임계와
신뢰 규칙을 사전 정의한다 — **임계 없는 게이트는 게이트가 아니다.**

채점에서 사람 눈을 뺄 수 있는 부분은 뺐다. `ShapeFidelity`가 경로 귀속을 거리 지표로 계산하고,
16장을 따로 열어 보지 않도록 `ContactSheetRenderer`가 한 장의 그리드 이미지로 묶는다.

## 문서에 신뢰도 라벨을 붙인다

[`DESIGN.md`](DESIGN.md)의 모든 주장에는 얼마나 믿을 수 있는지가 붙어 있다. 확인한 것과 추측한
것이 같은 문장처럼 보이면 나중에 어디부터 의심해야 할지 알 수 없다.

| 라벨 | 뜻 |
|---|---|
| `[조사확인]` | 1차 자료로 확인. 출처는 §15 |
| `[기존지식]` | 모델 학습 지식. 착수 전 실측 권장 |
| `[설계]` | 이 문서의 판단. 대안 존재 |
| `[가설]` | 검증 안 된 제품 가정. **틀리면 방향을 바꿔야 하는 것** |
| `[미검증]` | 확인 못 함. 착수 전 실측 필수 |

## 구조

| 모듈 | 역할 |
|---|---|
| `ShapeKit` | 투영·정규화·형상 지문, 컨트롤 이미지와 컨택트 시트 렌더 |
| `RouteKit` | 위치 수집, 필터 체인, 기록 모드, 세션 저널 |
| `GenerationKit` | 생성 클라이언트, 잡, 쿼터 원장, 온디바이스 경로 |
| `RoutePicStore` | 스키마, 활동 리포지토리, 아트워크 저장, 카드 렌더 |
| `Server/proxy` | 생성 API 프록시 |

서버 경로와 온디바이스 경로는 같은 transport seam 뒤에 있다. 온디바이스는
[`ml-stable-diffusion`](https://github.com/apple/ml-stable-diffusion)으로 ControlNet 조건부
생성을 기기에서 돌리고, 과금 대상이 아니라 쿼터 예약·확정·환불을 통째로 건너뛴다.

Swift 75파일, 테스트 함수 295개.

## 상태

작업 중이다. `ShapeKit`·`RouteKit`·`GenerationKit`·`RoutePicStore` 코어와 스파이크 2회가
끝났고, 제품 가설(SP-1)은 아직 검증되지 않았다. 설계서에서 `[가설]`과 `[미검증]`이 붙은 항목이
남은 리스크다.

## 문서

- [`DESIGN.md`](DESIGN.md) — 설계서. v0.6까지의 개정 이력과 각 개정이 무엇 때문에 바뀌었는지 포함
- [`PLAN.md`](PLAN.md) — 구현 계획. 마일스톤별 DoD
- [`OnDevice/SPIKE-RESULTS.md`](OnDevice/SPIKE-RESULTS.md) — 온디바이스 생성 실측 (seed 스윕 6건, 강도 축 42건)
- [`OnDevice/README.md`](OnDevice/README.md) — 온디바이스 경로가 왜 열려 있는가
